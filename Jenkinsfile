pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
        echo 'Running test stage webhook test commit'
      }
    }
    stage('Build') {
      steps {
        echo 'Running build stage'
      }
    }
  }
}
